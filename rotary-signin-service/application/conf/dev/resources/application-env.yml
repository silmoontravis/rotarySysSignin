spring:
    profiles: dev
    datasource:
        master:
            driver-class-name: com.mysql.cj.jdbc.Driver
            jdbcUrl: jdbc:mysql://ubuntu2004.wsl:3306/smd-rotary?characterEncoding=utf8&connectTimeout=1000&socketTimeout=3000&autoReconnect=true&useUnicode=true&useSSL=false&serverTimezone=UTC
            username: root
            password: password
server:
    port: 8080
swagger:
    enabled: true
logging:
    level:
        root: info
    file:
        path: /var/log/app
